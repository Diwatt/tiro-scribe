/* eslint-disable */
// Generated by @hey-api/openapi-ts. Exempt from project MDC. Do not edit.

export type ClientOptions = {
    baseURL: `${string}://${string}/api` | (string & {});
};

/**
 * One model artifact variant for a use_case. When default is true, client must use this variant without searching by app language (language-agnostic). Otherwise (default false) client selects by app language.
 *
 */
export type ModelArtifactVariant = {
    /**
     * Primary language tag only (lowercase), or the literal "multilingual".
     * Must match the client app locale format: expo-localization is normalized to primary tag (e.g. en, fr) via AppLanguage.
     * Use "en", "fr", etc. for language-specific variants; use "multilingual" for language-agnostic model artifacts (VAD, pitch, speaker).
     * Do not use region subtags (e.g. prefer "en" not "en-US") to avoid client mismatch.
     *
     */
    language: string;
    /**
     * When true, use this variant as-is; do not search by app language. When false, variant is chosen only when app language matches.
     */
    default: boolean;
    id: string;
    version: string;
    url: string;
    hash: string;
    size_bytes: number;
    min_app_version: string;
};

/**
 * One model artifact entry per use_case. Always has a variants array (one or more). Client picks one variant by app language when needed; single-variant entries (e.g. VAD) have one item in variants.
 *
 */
export type ModelArtifact = {
    /**
     * Model artifact use case (e.g. speaker_id, vad, asr)
     */
    use_case: string;
    variants: Array<ModelArtifactVariant>;
};

export type SelectOption = {
    /**
     * Option value (e.g. code or id)
     */
    value: string;
    /**
     * Display label
     */
    label: string;
};

export type ClinicalAttributes = {
    qualifications: Array<SelectOption>;
    therapyMethods: Array<SelectOption>;
    languages: Array<SelectOption>;
};

export type GetClinicalAttributesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Preferred language for labels (e.g. en, fr). Server returns labels in this locale when supported.
         */
        locale?: string;
    };
    url: '/clinical-attributes';
};

export type GetClinicalAttributesResponses = {
    /**
     * Clinical attributes for app bootstrap (labels in requested locale when provided)
     */
    200: ClinicalAttributes;
};

export type GetClinicalAttributesResponse = GetClinicalAttributesResponses[keyof GetClinicalAttributesResponses];

export type GetArtifactsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/artifacts';
};

export type GetArtifactsResponses = {
    /**
     * Array of model artifacts (look up by use_case)
     */
    200: Array<ModelArtifact>;
};

export type GetArtifactsResponse = GetArtifactsResponses[keyof GetArtifactsResponses];
